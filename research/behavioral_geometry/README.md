# Behavioral Continuation Geometry

This package tests whether a latent state must be judged by its multi-step
continuation distribution rather than by raw coordinates or only the next
token distribution.

## Current research decision

The old coordinate-first FCTR/CETR headline is permanently stopped. The
horizon-sufficiency branch remains as a preserved research audit, but its
no-JVP redesign is now `RECOMMEND-KILL-AS-AAAAI-MAIN`: Family A passed only an
engineering preflight, Family B lacks a real source-objective construction
group, and the proposed PaTR method cannot inherit the frozen P2 certificate
contract. The artifacts remain useful as a reproducible diagnostic baseline.

The P0 contracts are frozen, and calibration/held-out work remains `HOLD`. A
narrow, synthetic `GO-CHECKPOINT-PREFLIGHT` and its Linux runtime repair have
now been run. The repair passed real request/tokenizer/524 semantics but failed
the fixed-support numerical JVP gate; neither is a scientific checkpoint result
or an authorization for `GO-CAL`. An overlay alone is insufficient: GPU
calibration additionally requires frozen P0 contracts, a passed CPU/source
feasibility audit, a frozen stochastic P1 preregistration, a passed numerical
checkpoint-preflight repair, and the explicit stage authorization required by
that document (`GO-CAL`, then separate held-out gates).

P0 documents:

- [`P0_FREEZE_MANIFEST.md`](P0_FREEZE_MANIFEST.md): immutable P0 artifact
  hashes, review status, and the current authorization boundary;
- [`P0_CLAIM_CONTRACT.md`](P0_CLAIM_CONTRACT.md): exact claim ladder and
  authorization boundary;
- [`P0_MATHEMATICAL_SPEC.md`](P0_MATHEMATICAL_SPEC.md): normative distinction
  between factual paths, stochastic path samples, and expected trajectory
  Fisher;
- [`P0_COLLISION_MATRIX.md`](P0_COLLISION_MATRIX.md): claim subtraction against
  direct 2026 neighbors;
- [`P0_KILL_CRITERIA.md`](P0_KILL_CRITERIA.md): permanent stop, downgrade, and
  phase gates; and
- [`P0_SWITCH_C2_COMPATIBILITY.md`](P0_SWITCH_C2_COMPATIBILITY.md): why frozen
  C2 is only a factual greedy-prefix screen for the revised direction and is not yet worth
  a 96 GB GPU run.

Post-freeze feasibility work:

- [`P1_NO_JVP_REDESIGN_DRAFT_ZH.md`](P1_NO_JVP_REDESIGN_DRAFT_ZH.md): Chinese
  forward-only redesign draft. It replaces neither a frozen protocol nor a
  passed gate; it defines the proposed source-native action / shared-history
  continuation-KL route and its explicit kill conditions.

- [`P1_FORWARD_KL_PREFLIGHT_REPORT_ZH.md`](P1_FORWARD_KL_PREFLIGHT_REPORT_ZH.md):
  the endpoint-aware real-checkpoint no-JVP gate. The source-format synthetic
  reference and candidate both exit naturally through 524, independent-cache
  identity KL is exactly zero, fresh-cache forward/reverse same-history paths
  run, and a full H=64 cost control fits the local GPU. This authorizes only a
  separately frozen sacrificial-discovery protocol, not discovery execution,
  calibration, a Horizon-Gap claim, PaTR, or training.
- [`P1_FORWARD_KL_PREFLIGHT_SELF_PROMPT_ZH.md`](P1_FORWARD_KL_PREFLIGHT_SELF_PROMPT_ZH.md):
  the execution prompt, four engineering gates, endpoint vocabulary, and
  binding PASS/HOLD/FAIL decisions for the no-JVP preflight.
- [`P1_NO_JVP_DECISION_ADDENDUM_20260716_ZH.md`](P1_NO_JVP_DECISION_ADDENDUM_20260716_ZH.md):
  reconciles the earlier kill recommendation with the later source-format
  natural-exit result. The line is still not promoted as an AAAI method, but
  one locally capped sacrificial discovery may proceed after protocol freeze.
- [`results/P1_FORWARD_KL_PREFLIGHT_20260716.json`](results/P1_FORWARD_KL_PREFLIGHT_20260716.json):
  the complete machine-readable WSL/checkpoint result, including action bank,
  natural endpoints, identity, directional paths, timing, and VRAM.

- [`P1_NO_JVP_P2_METHOD_AUDIT_ZH.md`](P1_NO_JVP_P2_METHOD_AUDIT_ZH.md): the
  no-data mathematical audit of PaTR. It blocks PaTR as a current certificate
  or frozen-P2 continuation: its former per-action tail condition did not
  protect pairwise rankings, and a valid optional-stopping concentration
  package has not been established.

- [`P1_FAMILY_B_SOURCE_OBJECTIVE_AUDIT_ZH.md`](P1_FAMILY_B_SOURCE_OBJECTIVE_AUDIT_ZH.md):
  source-provenance audit for the proposed second action family. It is currently
  blocked because a real eight-rollout GRPO group, reward/advantage records,
  and the official actor runtime are absent; a synthetic advantage is explicitly
  disallowed as a replacement.

- [`P1_NO_JVP_FINAL_DECISION_ZH.md`](P1_NO_JVP_FINAL_DECISION_ZH.md): the
  final staged decision for the user's low-compute AAAI-main objective. It
  recommends stopping this line as a main-track investment while preserving all
  runtime and audit evidence.

- [`P1_RUNTIME_REPAIR_REPORT.md`](P1_RUNTIME_REPAIR_REPORT.md): the Linux
  source-runtime repair. Actual `Req.check_finished()` semantics pass, including
  the 524/additional-stop audit; the new fixed-support BF16 numerical JVP gate
  fails, so the binding result remains `NO-GO-CAL`.

- [`P1_CHECKPOINT_PREFLIGHT_REPORT.md`](P1_CHECKPOINT_PREFLIGHT_REPORT.md):
  local-checkpoint identity, tied-head, sampler, cache, 524-exit, GPU-memory,
  and JVP evidence. Its decision is `FAIL-CHECKPOINT-PREFLIGHT / NO-GO-CAL`;
  the required source-runtime and fixed-branch repairs are explicit.

- [`P1_CPU_FAKE_PREFLIGHT_REPORT.md`](P1_CPU_FAKE_PREFLIGHT_REPORT.md): passed
  CPU-only stop-order, source-derived and direct-source sampler, recursive
  source-adapter, source-likelihood, and JVP contract tests; it explicitly
  does not authorize checkpoint work.
- [`P1_SOURCE_ONLY_COMPLETION_AUDIT.md`](P1_SOURCE_ONLY_COMPLETION_AUDIT.md):
  source-only completion ledger and explicit `NO-GO-CHECKPOINT-PREFLIGHT`
  decision.
- [`P1_DESIGN_REVIEW_MANIFEST.md`](P1_DESIGN_REVIEW_MANIFEST.md): hashes and
  records the three-way `PASS-DESIGN / BLOCK-EXECUTION` snapshot without
  authorizing checkpoint work.
- [`P1_SOURCE_FEASIBILITY_AUDIT.md`](P1_SOURCE_FEASIBILITY_AUDIT.md): source,
  natural-update, compute, memory, and sample-size audit leading to
  `GO-REDRAFT / NO-GO-EXECUTION` for a separate stochastic P1.
- [`P1_REDTEAM_RESOLUTION.md`](P1_REDTEAM_RESOLUTION.md): independent
  source/estimand, mathematics/statistics, and AAAI-positioning verdicts and the
  binding resolution of the blocked first draft.
- [`P1_COLLISION_ADDENDUM.md`](P1_COLLISION_ADDENDUM.md): binding post-P0
  subtraction against the closest 2026 neighbors and the narrow residual claim
  the project is allowed to own.
- [`P1_STOCHASTIC_NATURAL_UPDATE_PREREGISTRATION_DRAFT.md`](P1_STOCHASTIC_NATURAL_UPDATE_PREREGISTRATION_DRAFT.md):
  non-executable v2 draft for first-action deterministic recursive closure,
  natural exploration and source-objective families, full-support expected KL,
  trace stability, power, specificity, and effect gates.
- [`P1_ADAPTED_METRIC_BASELINE_CONTRACT.md`](P1_ADAPTED_METRIC_BASELINE_CONTRACT.md):
  non-executable draft of the mandatory cross-checked adapted-metric
  tangent-growth competitor; it passed design review but remains pending
  fake-model validation, compute measurement, implementation review, and final
  execution freeze.

## Estimator contract

For a directional comparison `KL(P || Q)`, the evaluator:

1. samples a continuation from `P`;
2. teacher-forces `Q` on exactly the same token history;
3. computes the categorical KL at every shared prefix;
4. sums the terms using the autoregressive chain rule.

This is a Monte Carlo estimator of finite-horizon joint continuation KL. It is
not the same as comparing logits along two independently sampled paths. If an
adapter reports a sampling temperature, logits are divided by that temperature
before KL computation so the density matches the distribution that generated
the trajectory. Deterministic **visible** decoding is rejected because it does
not define the required stochastic continuation-policy KL. Deterministic
internal latent closure is allowed when the visible law remains stochastic and
the protocol explicitly audits support, proxy, and exit boundaries.

The first real-checkpoint experiment is an integration contract only. A later,
frozen phenomenon gate must show that multi-step geometry predicts a meaningful
failure that next-token KL and local coordinate baselines miss before any
amortized BCG method is built.
